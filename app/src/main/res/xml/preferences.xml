<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent">

    <CheckBoxPreference
        android:key="pref_nightmode"
        android:title="@string/pref_nightmode_title"
        android:defaultValue="false" />

    <ListPreference
        android:dependency="pref_filter"
        android:key="pref_filterType"
        android:title="@string/pref_filter_title"
        android:dialogTitle="@string/pref_filter_dialog"
        android:entries="@array/pref_filter_entries"
        android:entryValues="@array/pref_filter_values"
        android:defaultValue="@string/pref_filter_default" />
</PreferenceScreen>